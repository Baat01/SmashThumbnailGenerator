/**
 * ThumbnailCanvas — DÉPRÉCIÉ
 *
 * Ce composant a été remplacé par FabricThumbnailCanvas.jsx
 * qui utilise le moteur Fabric.js natif de SmashThumbnailGenerator.
 *
 * Conservé comme référence uniquement — non utilisé.
 */
export { default } from './FabricThumbnailCanvas';
